# 口播剪辑功能创作历程梳理

## 1. 从扣子工作流开始

- 起点是分析 `Workflow-cutcut_1-draft-2414.zip` 里的扣子工作流。
- 目标不是继续依赖扣子平台，而是参照它的流程和逻辑，沉淀成一个可复用的本地 Codex skill。
- 明确去掉剪映小助手插件环节，直接生成最终 MP4，不再输出剪映草稿。

## 2. 技术路线选择

- 早期讨论过 Hyperframe 和 Remotion。
- 最终选择 Remotion，因为口播成片需要字幕、花字、BGM、关键词音效等可组件化渲染能力。
- Remotion 模板负责根据 manifest 渲染最终 MP4，Python 和 ffmpeg 负责预处理、切割和素材整理。

## 3. 输入契约标准化

- 视频和文案被确定为必填输入。
- 不再要求用户输入横竖屏参数，而是通过 `ffprobe` 自动识别视频方向。
- 火山音字对齐优先，用户不提供火山鉴权或已有 word-level alignment 时，才 fallback 到本地 Whisper。
- 用户私有鉴权可以保存在本机 `private/credentials.local.json`，但分享 skill 前必须移除。

## 4. 气口剪辑核心突破

- 早期扣子版本的问题是只按句子级时间段处理，句子内部停顿、重复、卡壳经常剪不掉。
- 后来改为以 `utterances[].words[]` 为主信号，比较每个词的结束时间和下一个词的开始时间。
- 默认采用更适合短视频的 aggressive preset，较短停顿也会被纳入候选。
- 气口剪辑结果目前已经比较满意。

## 5. 字幕和花字迭代

- 字幕去掉半透明底纹，字号调小，生成字幕时不保留标点。
- 修复字幕从一行变两行时把花字顶上去的问题，花字位置不再依赖字幕行数。
- 早期关键词类型偏汽车样例，后来改为 LLM 优先基于语义抽取。
- 通用语义类型沉淀为 `entity`、`number`、`benefit`、`proof`、`risk`、`action`、`emotion`。

## 6. 声音资产接入

- 关键词音效按语义类型映射，要求短促、强调感强，并与关键词出现时间对齐。
- BGM 和 SFX 优先使用人工从 Pixabay 挑选并整理的本地素材索引。
- 每个音频资产保留来源、作者、许可、链接和 attribution 元数据。

## 7. Skill 文件夹补全

- 形成 `render-talking-video` skill。
- 关键文件包括 `SKILL.md`、`references/agent-runbook.md`、`references/dependencies.md`、`references/highlight-planning.md`、`references/remotion-manifest.md`。
- 脚本包括 cut planner、apply cut plan、build manifest、prepare Remotion project、validate manifest、export attribution、share safety check。
- 依赖文件补充了 ffmpeg、ffprobe、python3、node、npm、Remotion 以及可选 Whisper。

## 8. MVP 完成标准

- MVP 当前完成标准是成功输出最终 MP4。
- 不再要求成片后的质量检查环节。
- 失败时保留 alignment、cut plan、manifest、highlight plan、attribution 等中间产物，方便下一轮继续调试。
